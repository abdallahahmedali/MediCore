@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewBag.Title = "Departments";
}


<div class="container bg-white p-5">
    <h2 class="mb-5">
        Service Types
        <span class="float-end">
            <a href="/Admin/ServiceType/Create" class="btn btn-medi"><i class="fa-solid fa-plus"></i> Add Service</a>
        </span>
    </h2>

    <div class="table-responsive">
        
    <table class="table table-hover">
        <thead>
            <tr>
                <th>#</th>
                <th>Service Type Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @if(ViewBag.ServiceTypes != null)
            {
                int i = 1;
                
                @foreach (var Service in ViewBag.ServiceTypes)
                {
                    <tr>
                        <td>@i</td>
                        <td>@Service["Service_Type_Name"]</td>
                        <td>
                            <a href="/Admin/ServiceType/Edit/@Service["Service_Type_Name"]" class="btn btn-info"><i class="fa-solid fa-pen-to-square text-white"></i></a>
                        </td>
                    </tr>
                    i++;
                }
            }
            
        </tbody>
    </table>
    </div>
</div>
